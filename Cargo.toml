[package]
name = "rust-xp-git2"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[lints.rust]
unsafe_code = "forbid"
# unused = { level = "allow", priority = -1 } # For exploratory dev.

[dependencies]
git2 = { version = "0.20.2", features = ["https", "ssh"] }
